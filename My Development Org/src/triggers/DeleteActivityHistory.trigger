trigger DeleteActivityHistory on Case (after insert) {
    //List<Case> emDelList = new List<Case> ();
    //for (Case em : Trigger.new)   // bulkified
     //  if (em.subject.contains('Email: Wir haben Ihre Anfrage erhalten - Thema'))
     //     emDelList.add(em);

    //delete emDelList;   // delete all qualifying emailMessages
}